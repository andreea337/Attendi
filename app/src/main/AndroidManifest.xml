<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.attendi">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Attendi">
        <activity android:name=".schedule_student"></activity>
        <activity android:name=".login_student" />
        <activity android:name=".normal" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".schedule_teacher" />
        <activity android:name=".class_info" />
        <activity android:name=".login" />
        <activity android:name=".attendi_mode" />
        <activity android:name=".class_info_student"/>
        <activity android:name=".check_normal"/>
    </application>

</manifest>